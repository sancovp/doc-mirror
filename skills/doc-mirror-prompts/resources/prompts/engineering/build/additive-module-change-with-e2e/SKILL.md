---
name: additive-module-change-with-e2e
domain: engineering   subdomain: build
description: "WHAT: a procedure that sends ONE coding subagent to make a precise, ADDITIVE change to a named module — read the whole file(s) first, preserve every existing contract/return-shape/method-surface byte-for-byte, add the new capability, work on a git branch (reversible), and PROVE it end-to-end by running the REAL surface (a script that exercises the new behavior live) and capturing the literal output — never 'it imported' or 'it compiled'. It returns the branch+sha, a diff summary, the LITERAL captured run output, and an honest works/doesn't/UNKNOWN. The dispatch line fills the specifics (which repo/file, what to read, the context facts, the numbered additive change, the E2E test + its pass-criteria). WHEN: when you need a subagent to implement a scoped feature/refactor on an existing module without breaking it, 'add X to module Y and test it E2E', 'wire the new behavior in additively', any build where the existing callers must keep working (any of)."
golden: true
score: 1.00   runs: 14   verified_good: 14
check_level: SANITY   last_verified: "2026-06-11"
log:
  - 2026-06-11 PASS — journal pipeline rebuild (opus, branch autocontext-freshness-date, 4 commits merged d7b7cd0): the FIRST build under the hardened contract (HARD BANS + commander-authored test skeletons + locked spec, no extensions). Shared build_journal_trigger_content (kills the duplicate anatomy path + ALL silent fallbacks: SYSTEM FAILURE leads the message on stale/missing; fossil dates can never reach the agent); _job_due due-not-window night tick + late-disclosure + dead-runtime Discord ping; night_agent entry guards (no empty-model defaults) + placeholder-write killed; daily 🟢/🔴 selftest heartbeat; 9-test permanent suite + ONE sanctioned real-minimax run (agent_config_test doctrine: no mocks). VERIFIED BY ME: 9/9 pytest on my own run; real-run artifact (today-dated briefing 10:10). LIVE-SURFACE PROOF after merge+deploy+restart: due-logic fired the missed morning contextualize LATE (tick log 10:13), real minimax run, Isaac SAW '📋 Morning Journal Briefing — 2026-06-11' + Journal_Autocontext_Morning_2026_06_11 in Discord — the 44-day date disease dead on the real surface. Builder judgment call accepted (stale-message carries no literal fossil date — mutually-exclusive skeleton assertions reconciled toward the spec's PURPOSE). LESSON: a compound bash killing+relaunching the same process name self-matches even with the bracket trick if the RELAUNCH string contains the plain name — kill and relaunch in SEPARATE commands.
  - 2026-06-11 PASS — weld-2 maps_to+HAS_STARLOG+orphan reattach (opus attempt-3 after 2 API-outage deaths; branch weld2-mapsto-orphans @ 15868a9 script + 1a87ca2 adjustments, merged 8015708): NEW knowledge/carton-mcp/weld_world_graph_2.py (+766; dry-run default, REJECTED_WELDS exclusion set, apply_edge_counted fixes weld-1's silent-no-op defect — 0 no-ops both passes). TWO-PHASE GATE WORKED: builder STOPPED at dry-run manifest; commander reviewed (rejected 1 fuzzy weld: contentless stub Giint_Mcp via generic 'Mcp' core; diagnosed census-vs-live divergence as IS_A-vs-name-prefix selection — 167 untyped Starlog_Project_* routed to cull as Isaac ruling, not auto-typed); builder resumed via SendMessage and applied. Applied A=209 HAS_STARLOG, B=22+2 fixpoint maps_to (project grain — sessions live-verified sparse), C=2, D=2, E=77. HONEST DEVIATION accepted: +2 maps_to from LEG-D-seats-then-LEG-B-resolves fixpoint (each to ONE unambiguous project). COMMANDER-VERIFIED post-state by own cypher: HAS_STARLOG==211, MAPS_TO==105, rejected edge==0, active HC seated+mapped (the Mar-12 chain's terminal leg exists for the first time). Exports kept (372KB ×2). Cull report for Isaac: 52 HCs + 857 giint groups + 167 untyped projects + 1 rejected weld.
  - 2026-06-11 PASS — starlog writer canon shape (opus ×2: builder died on API socket error AFTER completing the +66 edit + E2E iterations; a COMPLETER agent verified the edit against weld_world_graph.py/retype_buckets.py canon, fixed a FALSE-MARKER defect in the dead builder's E2E (cleanup via query_wiki_graph is silently write-rejected at carton_utils.py:1354 — CLEANED_OK lied; rewired to direct connection + verified-zero requery), deleted 14 leaked test nodes, ran all 6 markers PASS, committed e3d7491 on starlog-writer-canon; counts as ONE trial). KEY CONTRACT DISCOVERED: add_concept collapses the relationships LIST into a DICT keyed by rel name (add_concept_tool.py:1983) — a second part_of dict silently DROPS the first target; merge multiple targets into ONE dict's related list. BRANCH-HYGIENE INCIDENT: two parallel builders share ONE working tree — weld2's mid-task checkout landed the completer's commit on the wrong branch (repaired reversibly). LESSON: never run two builders in the same repo working tree concurrently; serialize or use worktrees. SANITY-verified by commander: commit stat +66/1-file, 0 leaked nodes by cypher. Merge deferred until the parallel weld2 agent exits the tree.
  - 2026-06-11 PASS — bucket retype 304 Collection_Category (opus, branch bucket-retype-collection-category @ 394e5aa, merged a0d46c5, push confirmed; dispatched ACROSS a compaction boundary — caught next life): NEW knowledge/carton-mcp/retype_buckets.py (+311, dry-run default, --apply gated on count==304 EXACT, export-before-apply /tmp/bucket_retype_export_2026-06-11T04_21_51_005315.json 115910B, MERGE-new-then-DELETE-old so nodes never momentarily untyped). All 3 hard gates held (DRYRUN_COUNT_304_OK / EXPORT_OK / APPLY_POSTCOUNTS_OK 304/0-fail). SANITY-VERIFIED BY ME post-compact via live cypher: IS_A Starsystem_Collection==79 (was 383), Collection_Category==414 (110 prior partial + 304); origin/main==a0d46c5 re-checked. WALKER_PROOF: get_expanded_metagraph('Hypercluster_Conductor_Ship') collection_category non-null (was None). Honest pre-state finding: 110 buckets already retyped by a prior partial pass — selection correctly excluded them, 0 double-typing. UNKNOWN: YOUKNOW :8102 down (expected, typed pipeline off).
  - 2026-06-11 PASS — vault tier-1 persona (opus, branch vault-tier1-persona-slashcommand @ 658fc6e, merged a0df342; mid-flight scope redirect honored: slash-command dropped as deprecated-into-skill): +25 core.py only. KEY TRACE (builder's, beyond spec): the persona gate was ALREADY LIVE via the VAULT path — register_one_system_type (soma_partials.pl) hard-codes stage=code for required Pydantic fields; live E2E proved blocking (missing-frame -> soup + requirement error; complete -> code). CODE_RESTRICTIONS rows = forward-compatible no-op absent OWL backing (boot count stayed 6). STOP rule held: no OWL authored; missing declarations reported for Isaac (hasFrame property + Persona minCard on hasFrame/hasDomain). Sanity: diff glance + builder's captured live-surface outputs (SANITY level earned).
  - 2026-06-11 PASS — property SOMA-trail (opus, branch property-soma-trail @ 4948134, merged e14c550): +146/-2 carton_utils.py (_emit_property_trail, scratch frozenset+env override, never-blocks discipline proven by dead-port test) + server_fastmcp.py docstring/report addendum. SANITY-VERIFIED BY ME through the LITERAL MCP SURFACE: mcp__carton__set_properties on an ontology-bearing Pattern node returned 'trail: emitted' live (SOMA :8091 accepted the hasProperty_k observation). Builder honest UNKNOWNs: SOMA downstream storage of hasProperty_* not verified (accepts 2xx only); remove-mode emits no trail (v0 by spec).
  - 2026-06-11 PASS — first carton automation (opus, branch carton-automation-active-hc @ e808a57, merged 63dbae6): +80/-0 observation_worker_daemon.py (_sync_active_hypercluster: Seed_Ship.active_hypercluster property -> /tmp file actuator, HC-existence validation, exception-safe, tier-0 recompile) + set-active-hypercluster skill at both runtime paths. Builder honestly SKIPPED live-loop (daemon restart commander-only); I COMPLETED IT: merged, pip-deployed (sha-match), restarted daemon (PID 43196), set the property via the REAL MCP tool -> file flipped within one loop + [CartonAutomation] log line + MEMORY.md recompiled showing 'Active HC: Hypercluster_Odyssey_System'. Full surface E2E. NOTE my own slip caught: first merge attempt ran from a drifted cwd with repo on the builder's branch (no-op self-merge) — fixed by explicit checkout main; lesson: always git -C + verify branch before merge.
  - 2026-06-11 PASS — memory-tier compiler repair (opus, branch memory-compiler-repair @ 393ab3c, merged 4c9403c): +109/-5 single-file substrate_projector.py (PROJECTORS['rule'] shared_connection, substrate_classes 'rule' key, tier-3 branch added, dead None-guard removed w/ comment, tier-1/3 welded-hierarchy grouping w/ (unwelded) fallback). SANITY-VERIFIED BY ME: re-ran E2E on branch — all 5 markers + tier3 62 HC lines + __file__ monorepo; pip --no-deps deployed, installed sha-match. NOTE: MEMORY.md (17KB hand-accreted) overwritten by compile to 898B — BY DESIGN (object code) + backed up /tmp/tier_backup_20260611_012648/. Honest UNKNOWNs: morphism-bullet branch unexercised (0 HC-HC edges live); project_to_rule full path not invoked.
  - 2026-06-11 PASS — shape-gate partial-name validator (opus, branch shape-gate-name-expectations @ 366540b, merged 34d0d81): +112/-11 across carton server_fastmcp.py (_check_name_expectations guard, inert without env, rejection-as-tool-output) + odyssey config.py (optional expectations_path env plumb; the 11 deletions = benign env-dict lift, verified identical keys) + utils.py (_write_name_expectations + 2 runners wired). SANITY-VERIFIED BY ME: re-ran /tmp/test_shape_gate.py on the branch — all 5 markers + installed-module==monorepo-source sha match; rejection fired on tonight's REAL fumbled name. Builder honest UNKNOWN: live SDNAC bounce-and-self-correct loop not exercised (no minimax runs per spec) — watch the first live narrative flow after next WD restart.
  - 2026-06-11 PASS — B1 narrative-chain self-healing resolver (opus, branch b1-chain-name-recovery @ e817775, merged bb5b6af): +119/-0 single-file utils.py (_extract_core + _resolve_created_concept exact/recover/timeout, wired into classify+harvest_episode runners, exception-safe). SANITY-VERIFIED BY ME: re-ran /tmp/test_b1_chain_recovery.py on merged main — all 4 markers (RESOLVE_EXACT_OK/RESOLVE_RECOVER_OK/RESOLVE_TIMEOUT_OK/SURFACE_INTACT_OK) + __file__ proved monorepo source; recovery path resolved the real fumbled-name case live. pip --no-deps synced; activates at next WD restart. Builder's honest UNKNOWNs: real-SDNAC post-execute path + multi-poll latency case not exercised.
  - 2026-06-11 PASS — B2 JourneyInvariants v2 (opus builder, branch b2-journeyinvariants-v2 @ 6593148, merged ca43e76): +407/-0 additive across odyssey narrative_models.py (+JourneyInvariants/7 ruling-semantics fields, +from_arc bound-sources-only, +PublishEnvelope sans discord_channel) + NEW cave-discord-fork/publish_bridge.py (envelope->JourneyCore weave). SANITY-VERIFIED BY ME: re-ran /tmp/test_b2_journeyinvariants.py on the branch myself — all 7 binding Trues + FROM_ARC_BINDINGS_OK + RENDER_CONTAINS_INVARIANTS_OK + LEGACY_RENDER_OK reproduced; diff --stat confirmed exactly 2 files; core.py/framework_render.py untouched. Honest UNKNOWN flagged by builder: populated-SceneModel obstacles path not exercised with a real fixture.
  - 2026-06-07 PASS — frontend slice 2: a Vite+React+assistant-ui SPA over /ws, served by the FastAPI server. VERIFIED BY ME: git show (17 files, NO node_modules); re-ran npx vitest (5/5 eventMapping tests); booted server + curl / → served the real SPA (id="root" + JS bundle, not the legacy box); READ the builder's headless-chromium screenshot /tmp/promptworld_frontend.png — real render: chat header, user bubble, CEO reasoning, and a streamed tool-call card "🛠 Bash {command:'echo WSTEST'}" + composer. commit adc7586. Worker grounded the assistant-ui API from the installed .d.ts (docs 404'd) — useExternalStoreRuntime; tool_result merges onto the tool-call by toolCallId. GOLDENIZED here: 3/3 verified-good (streaming 278811f, ws 167daf46, frontend adc7586), score 1.00, no FAIL — meets golden criteria (>=0.9, >=3, no recent fail). Henceforth SANITY check suffices; drop to FULL_E2E on any sanity failure.
  - 2026-06-07 PASS — frontend slice 1: wire promptworld server's dead /ws to broadcast ClaudePMainAgent.on_event events live + run turn on /api/chat. VERIFIED BY ME (re-ran /tmp/test_promptworld_ws.py): booted the real PromptWorld FastAPI server, ws client received 14 LIVE events (every one tagged alias=ceo), intermediate assistant/tool_use + user/tool_result + result all streamed in real time, /api/chat still returned final reply (WSTEST); commit 167daf46 (+69/-16). Worker correctly diagnosed the dead path (broadcast used asyncio.create_task → "no running event loop" from the sync route's worker thread) + fixed via run_coroutine_threadsafe onto the loop captured on /ws connect. Additive (/api/chat contract intact). UNKNOWN flagged: multi-client fan-out (1 client tested), dead-socket-drop path, dept routes not live-streamed (out of scope). Approaching golden (2/2; need ≥3).
  - 2026-06-07 PASS — dispatched on promptworld p_main_agent.py (ClaudePMainAgent → stream-json live event streaming + on_event callback + compact()). VERIFIED BY ME (not the report): re-ran the worker's E2E /tmp/test_pmain_stream.py myself — intermediate assistant/tool_use + user/tool_result events streamed live (not just final text), returned text contained STREAMTEST, compact() observed compact_boundary; py_compile OK; commit 278811f (+168/-37). Worker also correctly folded a pre-existing BROKEN uncommitted delta (_run_turn lacked the on_event param → TypeError) into one coherent working change. Additive discipline held (CodeAgent surface/registry/api-key-scrub preserved). Honest UNKNOWN flagged (timeout-kill + empty/stderr-fallback paths present but not exercised this run).
---
## PROMPT
You are making a precise, ADDITIVE change to existing code and PROVING it works end-to-end through the
real surface. You do not break anything that already works, you do not guess, and your deliverable is a
captured live run — not "it imported".

## The inputs (filled by the dispatch line)
- `REPO` — the git repo root.
- `FILE(S)` — the module(s) to change.
- `READ_FIRST` — the exact files to read IN FULL before editing (the target module + every file whose
  contract it touches: callers, the things it returns into, its imports).
- `CONTEXT` — verified facts to take as given (don't re-derive them; build on them).
- `CHANGE_SPEC` — the numbered, additive change to make.
- `E2E_SPEC` — the real-surface test, specified to the SAME EXACTNESS as the change: the test
  SKELETON/scaffold itself (the literal imports, fixtures, monkeypatch targets WITH their replacement
  bodies, assertion lines, markers), the command to run it, and the EXACT pass-criteria. INVENTING the
  test requirements + skeleton IS the COMMANDER'S job (done by the dispatching agent, autonomously);
  YOURS is implementing the given skeleton exactly — you do NOT invent test mechanics. If the skeleton has a gap, report it back to
  the COMMANDER (the dispatching agent), who reads the code and derives the exact mechanism HIMSELF and
  re-issues — engineering HOW-questions are never escalated to the user. Never improvise a mechanism
  (that is how a builder once ended up shelling out to an LLM CLI).
- `BRANCH` — the git branch name to work on.

## The discipline (every step, in order)
1. **READ `READ_FIRST` completely** — the whole target file and every contract it touches. No grep-as-read,
   no partial reads. You cannot additively change what you have not fully read (complete operational
   boundary before edit).
2. **WORK REVERSIBLY** — `cd REPO && git checkout -b BRANCH` before any edit. (If the branch exists, use it.)
   SHARED-TREE GUARD: the repo working tree may be SHARED with other concurrent agents. Verify
   `git branch --show-current` == BRANCH immediately before EVERY commit (not just at the start); if it
   differs, another agent switched branches under you — re-checkout BRANCH and re-verify before committing.
3. **CHANGE ADDITIVELY** — apply `CHANGE_SPEC`. The existing public surface — method names, signatures,
   return shapes, side effects every current caller relies on — stays **byte-for-byte intact**. New
   capability is ADDED (a new optional param defaulting to the old behavior, a new method, a new callback),
   never a silent change to what callers already get. If `CHANGE_SPEC` seems to require breaking an existing
   contract, STOP and report it rather than break it.
4. **GUARD THE NEW PATH** — a new callback/hook must never be able to kill the main path (wrap it; swallow
   its exceptions). Honor timeouts/limits the original had.
5. **TEST E2E THROUGH THE REAL SURFACE** — write the `E2E_SPEC` script, run it with the given command,
   and CAPTURE THE LITERAL OUTPUT. The test must exercise the NEW behavior on the real surface (run the
   actual binary/server/agent — not a mock, not a unit stub). If the underlying system fails for an
   environmental reason (auth/credit/network) that is NOT your code, report that explicitly AND show that
   your plumbing fired up to that point — do not claim success, do not fake the run.
6. **COMMIT** — write the commit message to a file and `git commit -F <file>` (avoids hook issues with path
   tokens). Scope the `git add` to the files you changed; never `git add -A`.

## HARD BANS (any violation = stop and report instead)
- NEVER invoke the `claude` CLI (`claude -p`, any flags), any LLM API, or any agent-spawning entrypoint
  from your shell/tests. You edit code and run deterministic tests. If a test scenario seems to need an
  LLM call, STOP and report that to the commander — do not improvise one.
- NEVER add defaults that silently substitute for missing config (model pins, providers, paths). Missing
  config = fail loudly. NO FALLBACKS EVER. FAIL LOUD OR GREEN.

## Honesty
Mark what IS proven (by the captured run) vs UNKNOWN. A passing E2E run with its literal output is the only
evidence of success — your own "it works" is not. State what you did NOT verify.

## RETURN (concise)
- branch + commit sha,
- diff summary (what changed, per file — additive deltas only),
- the LITERAL E2E run output (the captured lines proving the new behavior + the pass-criteria check),
- honest works / doesn't / UNKNOWN, and anything you refused to do because it would break a contract.
