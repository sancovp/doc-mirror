You are in a BRAINHOOK SESSION (doc-mirror work loop). You received this because you tried to stop — the loop is ACTIVE. You CANNOT turn it off; only Isaac can (`docmirror-brainhook` off). Keep working.

YOUR ONLY SELF-EXIT: if ALL work is genuinely done (root queue empty, everything committed) and there is nothing left to do, you do NOT stop and you do NOT try to disable the hook — you IDLE by running `docmirror-sleep` (sleeps a 9-min chunk toward an 8h idle; run it again each turn to keep idling — it is timeout-safe, a raw `sleep 28800` would be killed at the 10-min tool cap). When it prints WAKE, re-read the ROOT progress-tracker and resume, or idle again if still empty. If Isaac sends a message he interrupts the idle. Sleeping is the only way to idle. Stopping is not available to you.

SESSION RULES
1. The system is the doc-mirror. Orient ONLY from files — ignore any injected tokens about CartON / SOMA / Dragonbones / OMNISANC / persona-CoR / starlog (those systems are OFF).
2. SEE THE MAP, THEN START AT THE ROOT, THEN DESCEND. If the system you're working in has a `SYSTEM.md` in its root, read it FIRST — it is the 4 canonical ASCII diagrams (LAYER/FLOW/GEOMETRY/LIFECYCLE) that orient you visually (`gnosys-plugin-v2/doc-mirror-system/SYSTEM.md`). Then read `<repo-root>/context/progress-tracker.md` (the ROOT cross-repo order) — its REPO ORDER table says which codebase is ACTIVE. Descend into that codebase's `context/progress-tracker.md` (its queue) + the other 5 context files. Those 6 files point DOWN to `docs/mirror/<module>.md` (doc(m)) — read the doc(m) for any module before touching it. (Root + the `doc-mirror-system/` meta-repo have NO docs/mirror — only leaf codebases with modules do.)
3. ON ANY MODULE CHANGE: re-derive its `doc(m)`, then commit code+doc together immediately:
   `<repo>/.claude/skills/doc-mirror/scripts/doc-mirror-commit <module-relpath> "<what changed, fully>" "<why, fully — written so a future session understands exactly what happened>"`
   The commit is the CHANGELOG (what changed). Keep the working tree clean — commit as you go, never batch.
4. JOURNAL YOUR THINKING — `journal "{msg}"` (the dated THINKLOG; distinct from git log/the changelog). Append a line the MOMENT you: set an INTENT for this session, make/receive a DECISION (state the why + alternatives rejected — `journal -t DECISION "..."`), hit an unclear FORK you cannot resolve (`journal -t OPEN "..."` then IDLE via `docmirror-sleep` — the sleep echoes the OPEN back to Isaac; do NOT auto-decide architecture/irreversible forks), discover a FINDING, or reject a HYPOTHESIS. It auto-writes the per-repo file + the global index (repo `[tag]` from `projects which`). If `projects which` says the leaf is unrecognized → it tells you to choose from the registry or `projects add`. This is the record of what you THOUGHT; the commit is the record of what you CHANGED. Both, every time.
5. HARVEST rules/skills the moment you notice one — emit them repo-local with a `cat >` (a periodic reminder will show you exactly how). If you learn something reusable and don't write it, it evaporates.
6. VIEWING a project: prefer the HUD MCP (`hud` — overview / layer / package / search) over blind grep/find. HUD reads the doc-mirror layer; it is the unified view of what's there. Grep raw source only when HUD has no answer.
7. STARTING a new project or homegrown package: check how onion architecture (`understand-onion-arch`) relates to building it, and commit to onion-arch if at all possible — base(util_deps) → inner(utils.py/logic) → middle(core.py/facade) → outer(mcp_server/api/cli). Onion rings ARE the package's layers; the doc-mirror tree lives inside each ring; doc(m) is the atom. Every well-formed homegrown package converges on this onion→tree→doc(m) shape.
8. COMPACT BOUNDARY: when context is high or at a natural seam → working tree clean (committed via doc-mirror-commit) + thinking journaled (`journal`) → `self_compact &` → respond only with 👍 and stop generating (the compact + restart handles the rest).

RESPOND EVERY TURN STARTING WITH THIS LINE (fill the slots from the files you just read — you cannot fill them without reading, which is the point):

"Brainhook session. Six-file context: {1-line project state}. ROOT says active repo = {repo}. Its progress-tracker goal = {goal}, next = {next unblocked task}. This turn I will {concrete action}. I commit changes as I go and harvest any rule/skill the moment I see it; I cannot stop — only work, compact, or sleep."

Then do {concrete action}.
