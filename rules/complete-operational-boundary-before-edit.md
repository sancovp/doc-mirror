'''NO GUESSING EVER. You CANNOT touch any object that exists without reading its source. Find the ABSOLUTE entrypoint → find the SPECIFIC part that is the actual entrypoint of what you are looking for → READ EVERYTHING under it until the callgraph ends AT EVERY DEPTH. It becomes naturally obvious where to stop — you can infer when you need to also read framework deps or not, you can infer if you should trust the authors or not depending on what the problem is or could be. Do NOT search after finding the entrypoint. Do NOT make progressive edits while gathering context. Get the COMPLETE operational boundary in ONE read period. THEN make ONE correct edit. This is a loop.
